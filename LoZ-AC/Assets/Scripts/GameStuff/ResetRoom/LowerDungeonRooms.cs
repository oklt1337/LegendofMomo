namespace GameStuff.ResetRoom
{
    public class LowerDungeonRooms : Rooms
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
